# Cross-module tests

This directory is reserved for integration and end-to-end tests spanning
applications or bounded modules. Module-local tests should live close to their
own code when the application scaffold is introduced.

